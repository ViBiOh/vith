---
nameOverride: vith

image:
  repository: vibioh/vith

config:
  VITH_LOGGER_JSON: "true"
  VITH_READ_TIMEOUT: "1m"
  VITH_WRITE_TIMEOUT: "1m"

resources:
  limits:
    cpu: 100m
    memory: 1G
  requests:
    cpu: 100m
    memory: 1G
